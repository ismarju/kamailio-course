# ISMAR - ovo je jednostavniji topology hiding modul
loadmodule "topoh.so"

modparam("topoh", "mask_key", "SuperSecretKey")     # Set the key used to encode/decode the data.
modparam("topoh", "mask_ip", "192.168.254.2")       # use the public Ip for masking 
#modparam("topoh", "mask_callid", 1)
